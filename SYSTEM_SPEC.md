# Space Assault — プロジェクト重要情報・総合技術仕様書

本ドキュメントは、2Dアクションシューティングゲーム「**Space Assault**」のコアシステム、ゲームメカニクス、マルチプレイ通信仕様、およびインフラ構成に関する最重要情報を集約した仕様書です。

---

## 1. プロジェクト概要 & アーキテクチャ

本プロジェクトは、HTML5 Canvas + 純粋なバニラJavaScriptで構築された、レスポンシブ（PC / スマホ両対応）のリアルタイム2Dアクションシューティングゲームです。**完全無料（ランニングコスト0円）**かつ**専用サーバー管理不要（サーバーレス）**で運用されています。

```
                    ┌─────────────────────────┐
                    │      GitHub Pages       │
                    │  (HTML5 / CSS3 / JS)    │
                    └────────────┬────────────┘
                                 │
           ┌─────────────────────┴─────────────────────┐
           ▼                                           ▼
┌─────────────────────────┐                 ┌─────────────────────────┐
│  Firebase Firestore     │                 │   PeerJS (WebRTC P2P)   │
│  (クラウドデータベース) │                 │  (リアルタイム通信)     │
│                         │                 │                         │
│ ・スコアアタック記録    │                 │ ・スター型トポロジー    │
│ ・ボス討伐タイム        │                 │ ・ホスト権威モデル      │
│ ・PC / Mobile別集計     │                 │ ・味方蘇生・HP・技同期  │
│ ・Solo / Multi別集計    │                 │ ・最大5人同時プレイ     │
└─────────────────────────┘                 └─────────────────────────┘
```

### 主要構成ファイル
| ファイル | 役割・規模 | 概要 |
| :--- | :--- | :--- |
| `index.html` | UI / スタイル (~2,400行) | ゲーム画面、HUD、タッチ操作ジョイスティック、各種モーダル（ランキング・登録・ロビー） |
| `game.js` | ゲームコアロジック (~5,770行) | プレイヤー、スキル、敵AI、ボス、弾幕、当たり判定、描画ループ、ローカルランキング管理 |
| `multiplayer.js` | マルチプレイ (~1,750行) | WebRTC (PeerJS) によるP2P通信、ルーム管理、状態同期、味方蘇生システム |
| `bgm.js` | サウンドエンジン (~740行) | Web Audio APIシンセサイザーによるBGM動的生成（ホーム・通常・通常ボス・ハム神） |
| `server.ps1` / `leaderboard.json` | ローカル開発用サーバー | ローカルテスト時のHTTP配信用PowerShellスクリプト & ランキングデータ保存JSON |

---

## 2. プレイヤーシステム & リソース仕様

プレイヤーは4種のリソース（HP, EP, WP, UEP）を管理しながら戦います。

### 2.1 リソースパラメータ
- **HP (Hit Points)**: 初期値 `100` / 最大 `100`
  - 被弾時に1回あたり `20` ダメージ。
  - `0` になるとソロプレイでは即ゲームオーバー。マルチプレイ時は「DOWN（行動不能）」状態へ移行。
- **EP (Energy Points)**: 初期値 `100` / 最大 `100` (旧称: MP)
  - 自然回復: 毎秒 `2.0` EP 自動回復。
  - ボス戦時はドロップする「青いEPオーブ」取得で `+30` 回復。
  - 各種スキル発動に使用。
- **WP (Weapon Points)**: 初期値 `0`
  - 敵を撃破することで蓄積（通常敵撃破で `+1`、必殺一刀両断で `+5`）。
  - しょったー召喚（20 WP）や必殺技（100 WP）の発動に必要。
- **UEP (Ultra Energy Points)**: 初期値 `0`
  - 敵撃破で獲得。**10ポイント蓄積するごとに、最大HPと最大EPが恒久的に +1** され、同時にHP・EPが1回復する育成要素。

### 2.2 スキル & アクション一覧
| スキル / アクション | 消費リソース | リキャスト / 制約 | 詳細効果 |
| :--- | :--- | :--- | :--- |
| **通常射撃** | なし | 130ms間隔 | クリックまたはドラッグ方向に弾（威力1）を連射 |
| **追尾弾 (Homing)** | `3 EP` / 発 | 260ms間隔 (長押し可) | 最もHPの高い敵（またはボス）を自動追尾（威力1.5） |
| **ブリンク (高速ダッシュ)** | `10 EP` | 300msクールダウン | 指定方向へ瞬間移動（無敵フレーム付与）。移動経路及び終点の周囲敵に衝撃波ダメージ（150点加算） |
| **無敵ガード (Barrier)** | 毎秒 `10 EP` | 長押し継続 | 展開中、あらゆる弾・体当たりを完全無効化 |
| **味方召喚 (しょったー)** | `20 WP` + `50 EP` | 1000msクールダウン | 味方ミニオン（HP 50）を召喚。プレイヤーに追従し、3秒ごとに最寄りの敵へ射撃。被弾肩代わり（20ダメ受ける） |
| **必殺技「一刀両断」** | `100 WP` + `100 EP` | 必殺モード中ブリンク | 最大HP敵の背後へ瞬間ワープし一刀両断。**100ダメージ**の超絶威力、画面停止（ソロ時はスローモーション）、衝撃波発生 |
| **グレイズ (かすり)** | なし | 判定半径 57px | 敵弾の至近距離をすり抜けるとボーナス獲得 |

---

## 3. ゲームモード & 敵・ボス仕様

### 3.1 ゲームモード
1. **スコアアタック (SCORE_ATTACK)**:
   - 迫りくる雑魚敵群を倒しハイスコアを目指すエンドレスモード。
   - スコア10,000点到達時に中ボスが出現。
2. **ボスバトル (BOSS)**:
   - レベル1〜5の通常ボス「サイバーボス」との1対1（またはマルチ）決戦。討伐クリアタイムを競う。
3. **イベントボス (EVENT_BOSS)**:
   - 不死身の神「**ハム神**」に挑むチャレンジモード。

### 3.2 雑魚敵タイプ
- **通常敵 (Normal)**: HP 2、赤色。直線的に移動。撃破時 500点。
- **追尾敵 (Chaser)**: HP 3、アンバー色。緩やかにプレイヤーを追尾。撃破時 700点。
- **射撃敵 (Shooter)**: HP 3、紫色六角形。出現後1.1〜1.6秒後にプレイヤーへ狙撃弾を発射。撃破時 600点。
- **大型装甲敵 (Large Heavy)**: HP 5、ピンク色八角形。高耐久。撃破時 1,000点。
※マルチプレイ時は参加人数に応じて雑魚敵のHPが倍率強化されます (`ceil(baseHp * multiHpMult)`)。

### 3.3 ボス仕様
- **通常ボス (Boss)**:
  - 初期HP: 100 × プレイヤー人数。
  - 3秒周期で行動パターン（10連射バースト、拡散弾、突進等）を実行。
- **イベントボス「ハム神」 (EventBossHamGod)**:
  - 不死身仕様（HP 999,999）。撃破不能。
  - **ダメージ分散機構**: 与えたダメージは参加人数で除算 (`damage / 人数`)。
  - **激怒システム (Enrage)**: 累計実質ダメージが15蓄積するごとに怒りレベルが上昇し弾幕速度・頻度が激化。
  - 5秒周期で3パターンの攻撃をローテーション:
    1. 4種敵型弾 5連射バースト
    2. ランダム特大ひまわりオーブ
    3. 2秒間持続のプレイヤー追尾極太レーザー

---

## 4. マルチプレイ & P2P通信仕様 (PeerJS)

- **通信方式**: WebRTC DataChannel (Peer-to-Peer)。中央中継サーバー不要で通信遅延を極小化。
- **ネットワーク構造**: **スター型トポロジー (Star Topology)**。
  - **ホスト権威モデル**: ホスト端末が敵の湧き、弾幕、ボスの行動・HP判定の権威（Authoritative）を持ち、クライアントへ定期ブロードキャスト。
  - クライアントは自身の座標・操作・発射弾の当たり判定レポートをホストへ送信。
- **マッチング**: 4桁の合言葉（Passcode）によるルーム制。最大2〜5人まで設定可能。

### 4.1 ダウン & 蘇生（レスキュー）システム
マルチプレイでプレイヤーのHPが0になっても即終了せず、「DOWN」状態になります。
1. **救助行動**: 生存中の味方がダウンしたプレイヤーの半径60px以内に留まることで蘇生ゲージが進行。
2. **蘇生時間**: 3.0秒間の救護で復活。
3. **デスペナルティ**: 復活ごとに**最大HPが -20 減少**（最低1）。
4. **全滅判定**: 参加プレイヤー全員が同時にダウンした場合にのみゲームオーバー。

---

## 5. オンラインリーダーボード & インフラ仕様

### 5.1 構成
- **Webホスティング**: GitHub Pages（CDN配信、SSL標準対応）
  - 公開URL: `https://fj2323hw.github.io/space-assault/`
- **オンラインDB**: Google Firebase Firestore (プロジェクトID: `space-assault-game`)
  - コレクション: `leaderboard`
  - カテゴリ（ソロ / マルチ）、デバイス（PC / スマホ）、モード（スコア / ボスレベル別）でリアルタイム集計。
  - ローカル開発環境では `server.ps1` 経由の `leaderboard.json` へのローカルフォールバック機能を搭載。

### 5.2 保存スキーマ
```json
{
  "name": "PlayerName",
  "mode": "SCORE_ATTACK",     // "SCORE_ATTACK" または "BOSS"
  "score": 15400,             // スコアアタック時
  "time": 45.23,              // ボス戦クリアタイム（秒）
  "level": 1,                 // ボスレベル
  "category": "solo",         // "solo" または "multi"
  "isMulti": false,
  "device": "pc",             // "pc" または "mobile"
  "date": "2026-09-08",
  "timestamp": 1788864600000
}
```

---

## 6. 操作対応表

| 操作 | PC (キーボード & マウス) | スマートフォン / タッチ |
| :--- | :--- | :--- |
| **移動** | `W, A, S, D` または `十字キー` | 左下 仮想ジョイスティック |
| **照準・通常射撃** | マウス移動 ＋ 左クリック長押し | 画面タッチドラッグ |
| **高速ブリンク** | 右クリック | 右下 `⚡` ボタン |
| **追尾弾射撃** | `Q` または `F` キー長押し | 右下 `🎯` ボタン長押し |
| **無敵ガード** | `Space` または `B` キー長押し | 右下 `🛡️` ボタン長押し |
| **味方召喚** | `E` キー | 右下 `🤖` ボタン |
| **必殺技切替** | `R` キー (WP 100 + EP 100時) | 右下 `🔥` ボタン |
